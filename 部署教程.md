# AI Interview Agent 部署教程

本文档记录本地开发环境的部署方式，适用于 Windows + WSL/Ubuntu，也适用于普通 Ubuntu/Linux 主机。

## 一、环境要求

推荐环境：

| 环境 | 说明 |
|---|---|
| Windows 10/11 | 本地开发与前端运行 |
| WSL2 / Ubuntu 22.04+ | 后端容器运行环境 |
| Docker | 构建和运行后端服务 |
| Docker Compose V2 | 编排 FastAPI、PostgreSQL、Redis、Celery |
| Node.js 18+ | 运行用户端和管理端前端 |

## 二、后端环境变量

进入后端目录：

```bash
cd /mnt/d/AImianshi/ai-interview-agent/ai-interview-agent/ai-interview-backend
```

复制环境变量模板：

```bash
cp .env.example .env
```

编辑 `.env`：

```bash
nano .env
```

关键配置：

```env
ENV=development
API_PORT=8006

POSTGRES_USER=demo
POSTGRES_PASSWORD=demo123
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=ai_interview

REDIS_HOST=redis
REDIS_PORT=6379

DEEPSEEK_API_KEY=your-llm-api-key
DEEPSEEK_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
DEEPSEEK_MODEL=qwen-plus

DASHSCOPE_API_KEY=your-dashscope-api-key
KNOWLEDGE_EMBEDDING_MODEL=text-embedding-v3
KNOWLEDGE_EMBEDDING_DIM=1024
```

说明：

- `DEEPSEEK_*` 是历史变量名，实际可接入任意 OpenAI-compatible Chat API。
- `DASHSCOPE_API_KEY` 用于 embedding 和 RAG 向量化。
- `KNOWLEDGE_EMBEDDING_DIM=1024` 与数据库中的 `vector(1024)` 对应，不建议随意修改。
- `.env` 不应提交到 GitHub。

## 三、启动后端

推荐使用项目脚本：

```bash
cd /mnt/d/AImianshi/ai-interview-agent/ai-interview-agent/ai-interview-backend
sed -i 's/\r$//' deploy_ubuntu.sh .env
chmod +x deploy_ubuntu.sh
./deploy_ubuntu.sh
```

也可以手动执行：

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
docker exec ai-interview-app alembic upgrade head
docker exec ai-interview-app python scripts/create_first_admin.py
docker exec ai-interview-app python scripts/seed_position_templates.py
```

查看容器：

```bash
docker ps
```

健康检查：

```bash
curl http://localhost:8006/api/v1/config/health
```

成功时会返回：

```json
{
  "code": 200,
  "message": "Success",
  "data": {
    "status": "healthy",
    "services": {
      "api": "up",
      "database": "up",
      "redis": "up"
    }
  }
}
```

## 四、启动前端

Windows PowerShell 进入项目根目录：

```powershell
cd D:\AImianshi\ai-interview-agent\ai-interview-agent
```

使用辅助脚本启动两个前端：

```powershell
powershell -ExecutionPolicy Bypass -File .\start-frontends.ps1 -Backend http://localhost:8006
```

用户端：

```text
http://localhost:3000
```

管理端：

```text
http://localhost:3001
```

如果不用脚本，也可以分别启动。

用户端：

```powershell
cd D:\AImianshi\ai-interview-agent\ai-interview-agent\ai-interview-frontend
$env:VITE_API_TARGET="http://localhost:8006"
npm install
npm run dev
```

管理端：

```powershell
cd D:\AImianshi\ai-interview-agent\ai-interview-agent\ai-interview-admin
$env:VITE_API_TARGET="http://localhost:8006"
npm install
npm run dev
```

## 五、默认账号

管理端默认账号：

```text
admin@ai-interview.com
ai-interview&admin
```

用户端没有默认账号，需要自行注册。

用户注册密码要求：

```text
至少 8 位
包含字母
包含数字
包含特殊字符
```

## 六、导入题库和知识库

管理端访问：

```text
http://localhost:3001
```

题库导入：

1. 进入题库管理。
2. 导入 `题库和知识库文档（用于后台上传）` 目录中的 JSON 文件。
3. 导入后执行向量化。

知识库导入：

1. 进入知识库管理。
2. 上传 `.md`、`.txt` 或 `.pdf` 文档。
3. 等待解析、chunk 切分、embedding 生成与入库。

## 七、常用命令

进入后端：

```bash
cd /mnt/d/AImianshi/ai-interview-agent/ai-interview-agent/ai-interview-backend
```

启动容器：

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
```

停止容器：

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml down
```

重启后端服务：

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml restart app celery-worker celery-beat
```

查看后端日志：

```bash
docker logs --tail=120 ai-interview-app
```

执行迁移：

```bash
docker exec ai-interview-app alembic upgrade head
```

检查 pgvector：

```bash
docker exec ai-interview-postgres psql -U demo -d ai_interview -c "SELECT extname, extversion FROM pg_extension WHERE extname='vector';"
```

## 八、常见问题

### 1. Docker 镜像拉取失败

可以配置 Docker registry mirror，或使用网络代理后重试：

```bash
docker pull python:3.12-slim
docker pull pgvector/pgvector:pg16
docker pull redis:7-alpine
```

### 2. 端口被占用

如果 PostgreSQL 外部端口冲突：

```env
POSTGRES_EXTERNAL_PORT=15434
```

如果 Redis 外部端口冲突：

```env
REDIS_EXTERNAL_PORT=16382
```

修改 `.env` 后重启容器：

```bash
docker compose -f docker-compose.yml -f docker-compose.dev.yml down
docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
```

### 3. 前端请求失败

检查：

- 后端健康检查是否通过
- 前端 `VITE_API_TARGET` 是否指向后端地址
- `vite.config.js` 中 proxy 是否生效

### 4. AI 功能失败

检查：

- `.env` 中是否填入真实模型 API Key
- DashScope 是否开通文本向量服务
- 后端容器是否在修改 `.env` 后重启
